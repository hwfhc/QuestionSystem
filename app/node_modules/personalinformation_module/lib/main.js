module.exports = start;

function start(config){

    return (new API(config));
};

function API(config){
    this.config = config;
}

API.prototype.initUser = require('./initUser.js');

API.prototype.getPasswordOfUser = require('./getPasswordOfUser.js');
