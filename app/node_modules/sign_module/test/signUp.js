//test environment init
var expect = require('chai').expect;
var directory = '../../';

var config = {
    modules: []
};

//module load area
config.modules['sign_module'] = (require(directory + './sign_module'))(config);
config.modules['personalinformation_module'] = (require(directory + './personalinformation_module'))(config);
config.modules['saferman'] = (require(directory + './saferman'))(config);

describe('signUp',function(){

    it('user1,correct password and username',function(){

        let isSucceeded = config.modules['sign_module'].signUp('testman',123);
        expect(isSucceeded).to.be.a('boolean');
        expect(isSucceeded).to.be.equal(true);

    });


    it('user2,correct password and username',function(){

        let isSucceeded = config.modules['sign_module'].signUp('sky',123);
        expect(isSucceeded).to.be.a('boolean');
        expect(isSucceeded).to.be.equal(true);

    });
    /////this test have not completed
    /*it('user3,incorrect password and correct username',function(){

        let isSucceeded = config.modules['sign_module'].signUp('testman',123);
        expect(isSucceeded).to.be.a('boolean');
        expect(isSucceeded).to.be.equal(true);

    });*/

    it('user4,correct password and incorrect username',function(){

        let isSucceeded = config.modules['sign_module'].signUp('testman><',123);
        expect(isSucceeded).to.be.a('boolean');
        expect(isSucceeded).to.be.equal(false);

    });

    /////this test have not completed
    /*it('user5,incorrect password and incorrect username',function(){

        let isSucceeded = config.modules['sign_module'].signUp('testman',123);
        expect(isSucceeded).to.be.a('boolean');
        expect(isSucceeded).to.be.equal(true);

    });*/

});
