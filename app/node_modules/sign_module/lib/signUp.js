module.exports = signUp;

function signUp(username,password){
    //check if password and username legeal
    /////////////

    this.config.modules['personalinformation_module'].initUser(username,password);
}
